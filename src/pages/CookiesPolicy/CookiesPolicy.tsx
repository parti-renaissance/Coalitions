import React, { FunctionComponent } from 'react';
import LegalPage from 'components/LegalPage';

const CookiesPolicy: FunctionComponent = () => (
  <LegalPage titleKey="cookies_policy.title" contents={[]} />
);

export default CookiesPolicy;
